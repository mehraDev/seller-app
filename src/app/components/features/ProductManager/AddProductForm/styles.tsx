import styled from "styled-components";

const ImageInputWrapper = styled.div`
    align-self: center;
`;

const ImageSuggestionsWrapper = styled.div`
    font-weight: 600;
`;
export {ImageInputWrapper,ImageSuggestionsWrapper}